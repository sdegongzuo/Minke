# 独立运行目录

打包版默认使用执行文件旁的 `data` 目录；开发版使用项目内的 `.devdata`。启动 Minke 不再继承终端或系统设置的 `DSH_HOME`，也不会自动发现、合并用户目录中的 `.dsh`。

```text
Minke.exe
data/
  minke/          桌面配置、浏览器会话及缓存
  dsh/            Harness 会话、配置、凭据和存储
    profiles/web/ 独立安装的插件及依赖
    agents/       独立的用户级共享技能目录
    skills/       Harness 用户技能
    cache/        npm、pnpm 缓存和依赖存储
    home/         插件子进程的用户目录及应用配置
```

Minke 启动内置 Harness，访问地址为 `http://localhost:<空闲端口>`；服务仅绑定本机 IPv4 回环地址。外部 DSH 保留自己的数据和进程，可以同时运行。不同目录中的 Minke 也分别使用各自的 `data`；同一目录仍只允许一个桌面实例。

请安装在可写目录。无法建立运行目录时会停止启动，不会回退到用户目录。更新或替换程序时应保留原来的 `data`；Windows 安装器的版本目录可能变化，需要确认新执行文件与原数据目录的位置。

插件需要在 Minke 内单独安装和配置。子进程的 HOME、USERPROFILE、APPDATA、LOCALAPPDATA、XDG 目录和包管理器缓存均指向私有目录；全局 `~/.agents` 和外部 bundled-skill 环境设置不会自动加载。用户主动打开的项目仍可以提供项目级技能与指令。

存储设置中明确保存的自定义目录仍优先。若旧版本已被手动配置成外部 DSH 目录，请在存储设置中选执行文件旁的一个全新专用目录，并使用“作为全新目录使用”。原数据会保留。

这是运行状态与配置隔离，不是操作系统沙箱。用户显式配置的远程服务、外部模型服务或插件绝对路径仍按该配置访问。
